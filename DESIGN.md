# Ien Design System

> Category: Design & Creative
> 차분한 중립 표면, 선명한 파란 강조색, 연속 곡률, 명확한 정보 위계를 사용하는 제품 중심 디자인 시스템.

이 문서는 AI 디자인·개발 도구가 Ien 스타일의 웹 화면과 시각 자료를 일관되게 생성하기 위한 독립적인 디자인 명세다. 이 파일 자체를 색상, 타이포그래피, 간격, 형태, 컴포넌트와 상호작용의 유일한 기준으로 사용한다. 특정 코드베이스, 프레임워크, 라이브러리 또는 기존 컴포넌트를 전제로 하지 않는다. 대상 프로젝트의 기술 구조는 유지하되 시각적 결정이 충돌하면 이 문서의 규칙과 토큰을 우선한다.

## 1. 시각적 주제와 분위기

### 핵심 인상

- 현대적이고 차분하며 신뢰할 수 있는 제품 인터페이스를 만든다.
- 장식보다 정보 전달과 조작의 명확성을 우선한다.
- 넓은 여백, 낮은 채도의 중립 표면, 간결한 타이포그래피를 기본으로 한다.
- 파란 브랜드색은 주요 행동, 현재 선택, 핵심 정보에만 제한적으로 사용한다.
- 오류·성공·경고·정보는 색만 바꾸지 말고 아이콘, 문구, 형태 변화와 함께 표현한다.
- 모서리는 부드러운 연속 곡률을 사용한다. 기계적인 직각이나 과도하게 둥근 카드의 남용을 피한다.

### 디자인 원칙

1. **내용 우선**: 장식이 정보보다 먼저 보이지 않게 한다.
2. **의미 기반 색상**: 색상값을 직접 고르지 말고 역할 토큰을 선택한다.
3. **한 화면, 한 주요 행동**: 가장 중요한 행동만 강한 채움 버튼으로 표현한다.
4. **점진적 강조**: 크기, 굵기, 색상, 표면 순으로 필요한 만큼만 강조한다.
5. **상태의 명시성**: 기본, 포커스, 선택, 눌림, 비활성, 읽기 전용, 오류, 성공 상태를 구분한다.
6. **접근 가능한 조작**: 모든 조작 요소는 최소 44px의 터치 영역을 확보한다.

### 기본 화면 인상

- 라이트 모드는 깨끗한 흰 배경과 미세한 회색 표면 차이를 사용한다.
- 다크 모드는 완전한 검정 대신 푸른 기가 약한 짙은 회색 표면을 층별로 사용한다.
- 카드와 섹션은 그림자보다 배경색, 테두리, 여백으로 구분한다.
- 콘텐츠 밀도는 중간 이하로 유지한다. 요소 사이에 숨 쉴 공간을 둔다.

## 2. 색상 체계

색상은 아래의 의미 토큰 이름을 그대로 사용한다. 임의의 유사 색상을 새로 만들지 않는다.

### 라이트 모드

| 토큰 | 값 | 용도 |
|---|---:|---|
| `background` | `#FFFFFF` | 화면 기본 배경 |
| `surface` | `#FFFFFF` | 일반 카드와 입력 표면 |
| `surfaceRaised` | `#FFFFFF` | 다이얼로그와 떠 있는 표면 |
| `surfaceWeak` | `#F9FAFB` | 약한 구획과 비활성 배경 |
| `surfaceVariant` | `#F2F4F6` | 선택 전 컨트롤과 대체 표면 |
| `textPrimary` | `#191F28` | 제목과 핵심 본문 |
| `textSecondary` | `#4E5968` | 보조 본문과 설명 |
| `textTertiary` | `#8B95A1` | 힌트, 자리표시자, 부가 정보 |
| `textDisabled` | `#B0B8C1` | 비활성 콘텐츠 |
| `border` | `#E5E8EB` | 기본 구분선과 테두리 |
| `borderStrong` | `#D1D6DB` | 강조 테두리 |
| `overlay` | `rgba(0, 0, 0, 0.60)` | 모달 뒤 가림막 |
| `brand` | `#3182F6` | 주요 행동과 선택 |
| `onBrand` | `#FFFFFF` | 브랜드색 위 콘텐츠 |
| `brandWeak` | `#E8F3FF` | 약한 브랜드 배경 |
| `onBrandWeak` | `#3182F6` | 약한 브랜드 배경 위 콘텐츠 |
| `success` | `#03B26C` | 성공 |
| `successWeak` | `#F0FAF6` | 약한 성공 배경 |
| `warning` | `#FE9800` | 경고 |
| `warningWeak` | `#FFF3E0` | 약한 경고 배경 |
| `danger` | `#F04452` | 오류와 삭제 |
| `dangerWeak` | `#FFEEEE` | 약한 오류 배경 |
| `info` | `#18A5A5` | 정보 |
| `infoWeak` | `#EDF8F8` | 약한 정보 배경 |

`success`, `warning`, `danger`, `info` 위의 콘텐츠는 `#FFFFFF`를 사용한다. 각 약한 배경 위에는 같은 의미의 기본색을 사용한다.

### 다크 모드

| 토큰 | 값 | 용도 |
|---|---:|---|
| `background` | `#101318` | 화면 기본 배경 |
| `surface` | `#171B22` | 일반 카드와 입력 표면 |
| `surfaceRaised` | `#202631` | 다이얼로그와 떠 있는 표면 |
| `surfaceWeak` | `#11151B` | 약한 구획 |
| `surfaceVariant` | `#202632` | 대체 표면 |
| `textPrimary` | `#F2F4F6` | 제목과 핵심 본문 |
| `textSecondary` | `#D1D6DB` | 보조 본문 |
| `textTertiary` | `#8B95A1` | 힌트와 부가 정보 |
| `textDisabled` | `#6B7684` | 비활성 콘텐츠 |
| `border` | `#333D4B` | 기본 구분선과 테두리 |
| `borderStrong` | `#4E5968` | 강조 테두리 |
| `overlay` | `rgba(0, 0, 0, 0.70)` | 모달 뒤 가림막 |
| `brand` | `#64A8FF` | 주요 행동과 선택 |
| `onBrand` | `#FFFFFF` | 브랜드색 위 콘텐츠 |
| `brandWeak` | `#17365D` | 약한 브랜드 배경 |
| `onBrandWeak` | `#64A8FF` | 약한 브랜드 배경 위 콘텐츠 |
| `success` | `#3FD599` | 성공 |
| `successWeak` | `#113B2B` | 약한 성공 배경 |
| `warning` | `#FFBD51` | 경고 |
| `warningWeak` | `#4A3211` | 약한 경고 배경 |
| `danger` | `#FB8890` | 오류와 삭제 |
| `dangerWeak` | `#4A1D22` | 약한 오류 배경 |
| `info` | `#58C7C7` | 정보 |
| `infoWeak` | `#123A3A` | 약한 정보 배경 |

### 색상 적용 규칙

- 본문 배경에는 `background`, 카드에는 `surface`, 모달에는 `surfaceRaised`를 사용한다.
- 중립 텍스트는 중요도에 따라 `textPrimary`, `textSecondary`, `textTertiary` 순으로 낮춘다.
- 주요 버튼은 `brand`와 `onBrand`, 보조 버튼은 `brandWeak`와 `onBrandWeak`를 사용한다.
- 테두리 버튼은 투명 배경, 의미색 콘텐츠, 1px 의미색 테두리를 사용한다.
- 삭제처럼 되돌리기 어려운 행동에만 `danger`를 사용한다.
- 넓은 면적을 의미색 원색으로 채우지 않는다. 넓은 안내 영역은 약한 색상을 사용한다.
- 텍스트와 배경의 명도 대비를 확인하고, 작은 텍스트는 더 강한 대비를 선택한다.

## 3. 타이포그래피

별도 글꼴을 강제하지 않는다. 대상 플랫폼의 가독성 높은 기본 산세리프 글꼴을 사용한다. 숫자와 한글이 섞여도 안정적인 글꼴을 우선한다.

| 토큰 | 크기 | 행간 | 굵기 | 용도 |
|---|---:|---:|---|---|
| `display` | 30 | 38 | 굵게 | 큰 수치, 결과, 짧은 대표 문구 |
| `title1` | 24 | 32 | 굵게 | 화면 제목 |
| `title2` | 20 | 28 | 약간 굵게 | 주요 섹션 제목 |
| `title3` | 18 | 26 | 약간 굵게 | 카드와 하위 섹션 제목 |
| `body1` | 16 | 24 | 보통 | 큰 본문과 주요 입력값 |
| `body2` | 15 | 22 | 보통 | 기본 본문과 입력값 |
| `label1` | 14 | 20 | 약간 굵게 | 버튼과 큰 레이블 |
| `label2` | 13 | 18 | 약간 굵게 | 작은 레이블과 제어 요소 |
| `caption` | 12 | 16 | 보통 | 보조 설명과 메타데이터 |

크기와 행간의 단위는 `px`다. 사용자의 글자 크기 설정을 지원해야 하는 웹 구현에서는 같은 비율을 유지한 `rem`으로 변환할 수 있다.

### 타이포그래피 규칙

- 화면마다 `title1`은 원칙적으로 하나만 사용한다.
- 제목은 짧고 직접적으로 작성하며 두 줄을 넘기지 않는다.
- 본문 기본값은 `body2`, 중요한 설명은 `body1`을 사용한다.
- 버튼 텍스트는 `label1` 또는 `label2`를 사용하고 가운데 정렬한다.
- 자리표시자는 본문과 같은 크기를 사용하되 `textTertiary`로 낮춘다.
- 필수 입력 표시는 레이블 뒤에 `danger` 색상의 별표를 붙인다.
- 금액과 핵심 수치는 `display` 또는 `title1`로 강조하되 색상 강조를 중복하지 않는다.
- 전부 대문자로 된 레이블, 과도한 자간, 장식용 기울임꼴을 사용하지 않는다.

## 4. 간격, 형태와 깊이

### 간격 체계

| 토큰 | 값 |
|---|---:|
| `none` | 0 |
| `xxxs` | 2 |
| `xxs` | 4 |
| `xs` | 8 |
| `sm` | 12 |
| `md` | 16 |
| `lg` | 20 |
| `xl` | 24 |
| `xxl` | 32 |
| `xxxl` | 40 |

단위는 `px`다. 기본 배수는 4이며 2는 미세 조정에만 쓴다. 반응형 웹 구현에서 `rem`을 사용하더라도 토큰 사이의 비율은 유지한다.

### 모서리 반경

| 토큰 | 값 | 용도 |
|---|---:|---|
| `none` | 0 | 목록 내부 구분, 직선 구조 |
| `xs` | 4 | 작은 표시 요소 |
| `sm` | 8 | 작은 버튼과 키패드 |
| `default` | 12 | 기본 카드, 입력, 버튼 |
| `md` | 12 | 기본 반경의 별칭 |
| `lg` | 16 | 섹션, 검색창, 큰 카드 |
| `xl` | 24 | 다이얼로그와 큰 오버레이 |
| `full` | 999 | 배지, 탭, 원형 제어 요소 |

- 둥근 사각형은 가능한 경우 연속 곡률을 사용한다.
- 같은 컨테이너 안에서 반경 단계를 세 개 이상 섞지 않는다.
- 모든 카드를 알약 형태로 만들지 않는다. 알약 형태는 배지, 세그먼트, 짧은 버튼에 제한한다.

### 선과 깊이

| 종류 | 값 |
|---|---:|
| 가는 보조선 | 0.5 |
| 기본선 | 1 |
| 중간선 | 1.5 |
| 강조선 | 2 |
| 기본 표면 깊이 | 0 |
| 떠 있는 카드 깊이 | 4 |
| 플로팅 요소 깊이 | 12 |
| 오버레이 깊이 | 24 |

- 그림자는 최소화한다. 먼저 표면색과 1px 테두리로 계층을 만든다.
- 그림자가 필요하면 넓고 부드럽고 낮은 불투명도로 표현한다.
- 다크 모드에서는 그림자보다 표면 밝기 차이를 우선한다.

### 아이콘

- 아이콘 크기 단계는 12, 16, 20, 24, 32다.
- 기본 아이콘은 20, 주요 탐색 아이콘은 24를 사용한다.
- 선형 아이콘을 기본으로 하고 선택·상태 강조에만 채움 아이콘을 사용한다.
- 아이콘은 텍스트의 의미를 보조해야 하며 장식용 아이콘을 반복하지 않는다.

## 5. 레이아웃과 반응형 구성

### 기본 구조

- 화면은 `상단 앱 바 → 주요 콘텐츠 → 선택적 하단 행동 영역` 순으로 구성한다.
- 모바일 화면의 좌우 기본 여백은 16, 넓은 화면은 24 이상을 사용한다.
- 화면 제목과 첫 콘텐츠 사이에는 16~24를 둔다.
- 섹션 사이에는 24~32, 섹션 내부 항목 사이에는 8~16을 둔다.
- 관련 항목은 가까이, 관계없는 항목은 명확히 멀리 배치한다.
- 목록 행은 최소 44 높이를 확보하고, 주요 목록은 52~64 높이를 권장한다.

### 섹션과 카드

- 섹션 제목은 `textSecondary`, 설명은 `textTertiary`를 사용한다.
- 묶음이 필요한 섹션은 `surfaceRaised`, 16 반경, 선택적 1px 테두리를 사용한다.
- 카드 안쪽 기본 여백은 16, 조밀한 작은 카드만 12를 사용한다.
- 카드의 제목, 본문, 행동은 위에서 아래로 자연스럽게 읽히게 정렬한다.
- 동일한 중요도의 카드를 그림자 크기로 차별하지 않는다.

### 반응형 규칙

- 작은 화면에서는 한 열을 기본으로 하고 주요 행동은 전체 너비를 사용한다.
- 넓은 화면에서는 콘텐츠 최대 너비를 제한하고 중앙 정렬한다. 본문형 화면은 640~760, 대시보드는 1200 내외를 권장한다.
- 두 열은 각 열이 최소 280 이상 확보될 때만 사용한다.
- 가로 공간이 줄면 보조 정보, 보조 행동, 장식 순으로 축소하거나 아래로 이동한다.
- 안전 영역과 키보드 영역을 침범하지 않는다.

### 정렬과 밀도

- 텍스트는 기본적으로 시작점 정렬한다. 짧은 버튼과 상태 표지만 가운데 정렬한다.
- 수치 표는 자릿수를 비교하기 쉽도록 끝점 정렬한다.
- 한 화면에서 서로 다른 카드 폭과 불규칙한 여백을 임의로 섞지 않는다.
- 빈 상태는 화면 중앙에만 고정하지 말고 실제 콘텐츠가 시작될 위치와 흐름을 고려한다.

## 6. 컴포넌트

아래 컴포넌트는 코드에 이미 존재한다고 가정하지 않는 개념적 디자인 규격이다. 대상 웹 프로젝트에 같은 역할의 컴포넌트가 있으면 스타일과 상태를 이 규격에 맞추고, 없으면 사용하는 프레임워크의 관례에 따라 동등한 컴포넌트를 만든다. 특정 라이브러리나 코드 이름에 의존하지 않는다.

### 버튼

- 기본 버튼 크기: 작은 36, 중간 44, 큰 52 높이.
- 가로 안쪽 여백: 작은 12, 중간 16, 큰 20.
- 변형: 채움, 약한 채움, 테두리, 투명.
- 의미 톤: 중립, 브랜드, 성공, 경고, 위험, 정보.
- 주요 행동은 브랜드 채움, 보조 행동은 브랜드 약한 채움 또는 테두리, 낮은 우선순위는 투명 버튼을 사용한다.
- 위험한 최종 행동은 위험 채움이나 위험 테두리를 사용하며 확인 단계가 필요하다.
- 버튼에 아이콘과 텍스트를 함께 쓸 때 간격은 4~8을 사용한다.
- 로딩 상태에서는 크기를 유지하고 내용을 로더로 교체하며 중복 입력을 막는다.
- 비활성 채움 버튼은 `surfaceVariant`, 콘텐츠는 `textDisabled`를 사용한다.
- 아이콘 버튼은 최소 44 터치 영역을 갖고, 한 아이콘만 명확하게 표시한다.
- 플로팅 행동 버튼은 40, 56, 96 크기를 지원하지만 한 화면에 하나만 사용한다.

### 텍스트 버튼

- 크기 단계의 최소 높이는 28, 32, 36, 40, 44, 48이다.
- 변형은 기본 투명형, 화살표형, 밑줄형이다.
- 본문 속 보조 이동이나 낮은 우선순위 행동에 사용한다.
- 주요 제출 행동을 텍스트 버튼으로 표현하지 않는다.

### 입력 필드

- 텍스트 입력 필드의 변형은 상자형, 하단선형, 큰 상자형, 강조형이다.
- 상자형과 하단선형의 기본 세로 여백은 14, 큰 상자형은 18, 강조형은 22다.
- 기본 입력 글꼴은 `body2`, 큰 값은 `body1` 또는 상위 스타일을 사용한다.
- 기본 테두리는 `border`, 포커스는 `brand`, 오류는 `danger`, 성공은 `success`를 사용한다.
- 비활성 배경은 `surfaceWeak`, 콘텐츠는 `textDisabled`를 사용한다.
- 레이블은 `label2`와 `textSecondary`, 도움말은 `caption`과 `textSecondary`를 사용한다.
- 필수 입력은 레이블 뒤에 위험색 별표를 표시한다.
- 오류·성공 메시지는 필드 바로 아래에 배치하고 해당 의미색을 사용한다.
- 글자 수 제한이 있으면 오른쪽 아래에 현재 길이와 최대 길이를 표시한다.
- 검색 필드는 44 높이, 16 반경, 약한 회색 배경을 사용한다.
- 여러 줄 입력은 내용이 늘어날 공간을 확보하고 단일 행 입력과 같은 높이로 고정하지 않는다.

### 선택과 제어

- 스위치는 즉시 반영되는 이진 설정에만 사용한다.
- 체크박스는 복수 선택이나 동의 항목에 사용하고 레이블 전체를 누를 수 있게 한다.
- 세그먼트 제어는 2~5개의 상호 배타적 선택에 사용한다.
- 세그먼트 제어 높이는 작은 40, 큰 48이며 바깥 여백 4와 완전한 캡슐 형태를 사용한다.
- 선택된 세그먼트는 밝은 표면과 `textPrimary`, 미선택은 `textSecondary`를 사용한다.
- 탭은 화면의 동급 콘텐츠 전환에 사용하고 선택 상태를 색상과 표시선 또는 표면으로 함께 표현한다.
- 슬라이더는 연속값에만 사용하며 정확한 수치가 중요하면 현재 값 레이블이나 숫자 입력을 함께 제공한다.
- 별점은 크기와 변형이 달라도 선택값을 색과 채움 상태로 동시에 보여준다.

### 배지와 상태 표시

- 배지는 작은, 중간, 큰 크기와 채움, 약한 채움, 테두리 변형을 사용한다.
- 기본은 약한 채움이다. 강한 채움은 중요한 상태에만 사용한다.
- 작은 배지 여백은 가로 6·세로 2, 중간은 8·3, 큰 배지는 10·5다.
- 배지 문구는 한 줄의 짧은 명사 또는 상태로 작성한다.

### 자산 프레임

- 아이콘, 로고, 짧은 텍스트를 감싸는 프레임 크기는 32, 44, 56, 72다.
- 기본은 약한 의미색 배경과 같은 의미색 콘텐츠다.
- 원형 또는 16 반경의 둥근 사각형을 사용한다.

### 목록과 섹션

- 목록 행은 선행 영역, 제목·설명 영역, 후행 값·행동 영역으로 구성한다.
- 제목은 `textPrimary`, 설명은 `textSecondary`, 부가값은 `textTertiary`를 사용한다.
- 행 전체가 이동 동작이면 후행 화살표를 표시한다.
- 분리선은 기본적으로 `border` 1px를 사용하고 마지막 행 뒤에는 생략할 수 있다.
- 설정이나 정보 묶음은 섹션 제목, 둥근 컨테이너, 선택적 캡션 순으로 구성한다.

### 피드백과 오버레이

- 다이얼로그는 `surfaceRaised`, 24 반경, 화면 가림막을 사용한다.
- 다이얼로그는 제목, 설명, 선택적 콘텐츠, 행동 영역 순서로 구성한다.
- 행동은 좁은 공간이나 긴 문구에서 세로 정렬하고, 짧은 두 행동만 가로 정렬한다.
- 바텀 시트는 위쪽 모서리만 16으로 둥글게 하고 콘텐츠, 중간, 전체 높이 단계를 지원한다.
- 스낵바는 짧은 결과 알림에 사용하고 한 개의 선택적 행동만 제공한다.
- 진행 상태는 작업이 끝나는 시점을 알 수 있으면 진행 막대, 알 수 없으면 로더를 사용한다.
- 스켈레톤은 실제 콘텐츠의 구조와 비슷하게 만들고 과도한 반짝임을 사용하지 않는다.
- 결과 화면은 성공, 실패, 비어 있음, 정보 상태를 아이콘·제목·설명·행동 순으로 표현한다.

### 하단 주요 행동 영역

- 화면의 제출·저장·다음 행동은 하단 고정 영역에 배치할 수 있다.
- 기본적으로 큰 버튼을 사용하고 한 개면 전체 너비, 두 개면 우선순위에 따라 비율을 나눈다.
- 하단 영역은 콘텐츠를 가리지 않도록 같은 높이의 아래 여백을 확보한다.

## 7. 모션과 상호작용

### 시간 토큰

| 토큰 | 시간 | 용도 |
|---|---:|---|
| `instant` | 80밀리초 | 즉각적인 누름 반응 |
| `fast` | 160밀리초 | 색상, 불투명도, 작은 상태 변화 |
| `normal` | 240밀리초 | 일반 전환과 크기 변화 |
| `slow` | 360밀리초 | 화면 진입, 시트와 큰 오버레이 |

기본 가속 곡선은 `cubic-bezier(0.2, 0, 0, 1)`이다.

### 상태 토큰

- 비활성 불투명도: 0.38
- 눌림 오버레이: 0.10
- 포커스 오버레이: 0.12
- 선택 오버레이: 0.14
- 최소 터치 영역: 44 × 44

### 모션 규칙

- 모션은 상태 변화의 원인과 결과를 연결하는 데 사용한다.
- 버튼 누름은 짧은 축소와 즉각적인 색상 반응으로 표현한다.
- 선택 표시의 위치 이동은 부드러운 스프링을 사용할 수 있지만 과도하게 튀지 않게 한다.
- 다이얼로그는 짧은 페이드와 미세한 크기 변화를, 바텀 시트는 아래에서 위로 이동을 사용한다.
- 로딩 중에도 전체 레이아웃의 크기와 위치가 흔들리지 않게 한다.
- 반복 애니메이션은 로더와 스켈레톤에만 제한한다.
- 사용자가 동작 줄이기를 설정하면 이동과 크기 변화를 페이드 중심으로 단순화한다.

## 8. 문체와 브랜드 표현

### 문체

- 친절하고 직접적이며 짧게 쓴다.
- 사용자가 해야 할 행동과 그 결과를 먼저 말한다.
- 전문 용어와 내부 구현 용어를 사용자 문구에 노출하지 않는다.
- 버튼은 `저장`, `계속`, `확인`, `다시 시도`처럼 동작을 나타내는 말로 작성한다.
- 제목은 문장 부호 없이 간결하게 작성한다.
- 오류 문구는 문제와 해결 방법을 함께 제시한다.
- 사용자를 탓하거나 불안을 키우는 표현을 사용하지 않는다.

### 브랜드 성격

- **신뢰감**: 안정된 정렬, 예측 가능한 동작, 분명한 상태.
- **절제**: 색상과 효과를 꼭 필요한 곳에만 사용.
- **명료함**: 정보 위계와 행동 우선순위를 즉시 이해할 수 있음.
- **친근함**: 부드러운 곡률과 자연스러운 한국어 문구.
- **실용성**: 아름다움보다 사용성과 접근성을 먼저 충족.

### 이미지와 그래픽

- 제품 화면에서는 이미지가 콘텐츠 이해에 기여할 때만 사용한다.
- 일러스트는 단순한 형태와 제한된 팔레트를 사용하고 인터페이스보다 강하게 보이지 않게 한다.
- 사진 위 텍스트는 충분한 가림막과 명도 대비를 확보한다.
- 의미 없는 3차원 오브젝트, 무작위 그라데이션 구체, 장식용 별빛을 추가하지 않는다.

## 9. 금지 패턴과 생성 검수

### 금지 패턴

- 보라색·분홍색 중심의 임의 그라데이션을 기본 브랜드 표현으로 사용하지 않는다.
- 모든 카드에 강한 그림자, 유리 효과, 빛나는 테두리를 적용하지 않는다.
- 한 화면에서 여러 개의 강한 채움 버튼을 동등하게 강조하지 않는다.
- 의미색을 장식색처럼 섞거나 오류가 아닌 곳에 위험색을 사용하지 않는다.
- 정의된 간격과 반경 토큰 밖의 임의 값을 반복해서 만들지 않는다.
- 작은 회색 글씨로 핵심 정보를 표현하지 않는다.
- 아이콘만으로 낯선 행동을 설명하지 않는다.
- 자리표시자를 레이블 대신 사용하지 않는다.
- 터치 영역을 시각 크기와 같게 축소하지 않는다.
- 다크 모드를 라이트 모드의 단순 색상 반전으로 만들지 않는다.
- 긴 문장을 가운데 정렬하지 않는다.
- 실제 데이터 없이 의미 없는 수치, 차트, 사용자 이름을 과도하게 채우지 않는다.
- 이모지를 핵심 인터페이스 아이콘 대신 사용하지 않는다.

### 생성 전 점검

- 화면의 주요 목적과 주요 행동이 하나의 문장으로 설명되는가?
- 라이트 또는 다크 색상 토큰 중 한 체계만 일관되게 사용했는가?
- 제목, 본문, 보조 정보의 위계가 타이포그래피와 색상으로 구분되는가?
- 간격이 정의된 토큰 체계를 따르고 관련 요소가 시각적으로 묶이는가?
- 기존 Ien 컴포넌트로 표현할 수 있는 요소를 임의로 새로 만들지 않았는가?

### 생성 후 점검

- 모든 텍스트와 아이콘이 배경에서 충분히 읽히는가?
- 모든 조작 요소가 최소 44 × 44의 터치 영역을 갖는가?
- 포커스, 선택, 눌림, 비활성, 오류 상태가 구분되는가?
- 주요 행동과 위험 행동의 우선순위가 명확한가?
- 긴 문구, 큰 글꼴, 작은 화면에서도 겹침과 잘림이 없는가?
- 다크 모드에서 표면 계층과 의미색이 유지되는가?
- 불필요한 장식, 과도한 그림자, 임의 그라데이션이 없는가?
- 결과가 Ien의 차분함, 명료함, 절제된 친근함을 유지하는가?
